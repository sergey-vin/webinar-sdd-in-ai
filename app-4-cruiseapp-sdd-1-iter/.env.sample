# No environment variables are required for local development.
# The app uses mock data from public/data/ and has no backend dependencies.

# If you add a real API later, put keys here:
# API_BASE_URL=https://api.example.com
